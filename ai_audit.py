def generate_pitch(missing):

    pitch=[]


    if "online_planner" in missing:
        pitch.append(
            "Online funeral arrangement system"
        )


    if "chat" in missing:
        pitch.append(
            "24/7 AI grief assistant"
        )


    if "pricing" in missing:
        pitch.append(
            "Transparent pricing calculator"
        )


    if "lead_capture" in missing:
        pitch.append(
            "Lead capture optimisation"
        )


    if "appointment_booking" in missing:
        pitch.append(
            "Online consultation booking"
        )


    return pitch
